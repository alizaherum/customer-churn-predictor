version: '3'
services:
  api:
    build: .
    ports:
      - "8000:8000"
  streamlit:
    build: .
    ports:
      - "8501:8501"
    command: streamlit run app.py --server.port 8501 --server.address 0.0.0.0
    depends_on:
      - api